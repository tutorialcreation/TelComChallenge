CREATE TABLE userData(
    id INT AUTO_INCREMENT,
    customer FLOAT,
    experience_score FLOAT,
    engagement_score FLOAT,
    satisfaction_sum FLOAT,
    satisfaction_score FLOAT,
    satisfaction_prediction_scores FLOAT,
    class INT,
    PRIMARY KEY(id)
);

